import { Container, Typography } from "@mui/material";

export default function Home() {
  return (
    <Container sx={{ mt: 10 }}>
      <Typography>Hi This Is Home Page</Typography>
    </Container>
  );
}
